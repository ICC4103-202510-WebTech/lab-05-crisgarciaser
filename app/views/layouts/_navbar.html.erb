<nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-4">
  <div class="container-fluid">
    <%= link_to 'WhatsNew', root_path, class: 'navbar-brand' %>
    
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <% if user_signed_in? %>
        <ul class="navbar-nav me-auto">
          <li class="nav-item">
            <%= link_to 'Users', users_path, class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to 'Chats', chats_path, class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to 'Messages', messages_path, class: 'nav-link' %>
          </li>
        </ul>
      <% end %>
      
      <ul class="navbar-nav ms-auto">  
        <% if user_signed_in? %>
          <li class="nav-item">
            <span class="nav-link">Logged in as: <%= current_user.email %></span>
          </li>
          <li class="nav-item">
            <%= link_to "Sign Out", destroy_user_session_path, class: "nav-link",data: { turbo_method: :delete,turbo_confirm: "¿u sur?"} %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Sign In", new_user_session_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Sign Up", new_user_registration_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>